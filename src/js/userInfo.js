export default class UserInfo {
  constructor(age, sex, exercise) {
    this.age = age;
    this.sex = sex;
    this.exercise = exercise;
    this.lifeExpectancy = 0

  mercuryAge(years) {
    let mercuryYears = parseFloat((year/.24).toFixed(4))
    return mercuryYears
  }
  
} 